"use client";

import Link from "next/link";
import { usePathname } from "next/navigation";
import { signOut } from "@/app/login/actions";

const navItems = [
  { href: "/", label: "Dashboard", icon: "dashboard" },
  { href: "/clients", label: "Clients", icon: "person_book" },
  { href: "/inventory", label: "Inventory", icon: "inventory_2" },
  { href: "/staff", label: "Staff", icon: "groups" },
];

export default function Sidebar() {
  const pathname = usePathname();

  return (
    <aside className="fixed left-0 top-0 h-full z-40 hidden md:flex flex-col py-8 px-6 w-64 pt-24 bg-surface-container-low border-r border-outline-variant">
      <div className="flex items-center gap-3 mb-10">
        {/* eslint-disable-next-line @next/next/no-img-element */}
        <img
          src="https://lh3.googleusercontent.com/aida-public/AB6AXuDuKPOvHof4wwKgYpWuF2loJle0p3gS116o-kryy5zgRAyqM9iwG5ikCHF8Kgo_1YGpilDEpmUehI2c935z6ApALpDedHE0nNMUnspxzLA45kgNpNAHtJX2xTSWut4j9f534CXgdN5n4ZuzzmVx1OTauTLjzrRbcNjruI97o2g518lM1eOl40J2qKi0xeDo3M3xwaOeoFA9RKsBCso0fZUFDvEIC3P5khzLaYBYQAX0_jQcwzxqtnGixB7ZjozUDX69S3lQuctSj_gq"
          alt="Studio Director"
          className="w-10 h-10 rounded-full object-cover border border-outline-variant"
        />
        <div>
          <p className="text-label-caps font-label-caps text-primary">
            Master Atelier
          </p>
          <p className="text-body-sm font-body-sm text-on-surface-variant">
            Director Profile
          </p>
        </div>
      </div>

      <nav className="flex flex-col gap-2 flex-grow">
        {navItems.map(({ href, label, icon }) => {
          const isActive =
            href === "/"
              ? pathname === "/"
              : pathname === href || pathname.startsWith(href + "/");
          return (
            <Link
              key={href}
              href={href}
              className={`flex items-center gap-3 py-3 px-4 transition-all duration-200 ${
                isActive
                  ? "text-primary font-bold border-r-2 border-primary bg-surface-container-high translate-x-1"
                  : "text-on-surface-variant hover:bg-surface-container-high"
              }`}
            >
              <span className="material-symbols-outlined">{icon}</span>
              <span className="text-label-caps font-label-caps">{label}</span>
            </Link>
          );
        })}
      </nav>

      <form action={signOut} className="mt-auto">
        <button
          type="submit"
          className="w-full py-4 flex items-center justify-center gap-2 border border-outline-variant text-on-surface-variant text-label-caps font-label-caps tracking-widest hover:bg-surface-container-high transition-colors"
        >
          <span className="material-symbols-outlined text-sm">logout</span>
          SIGN OUT
        </button>
      </form>
    </aside>
  );
}
